import { Grid } from "@mui/material"

export const TopBuffer = (): JSX.Element => {
    return (
        <Grid
            sm={12}
            width={`100vw`}
            height={`100vh`}>
        </Grid>
    )
}